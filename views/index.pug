extends layout

block content
    a(href="/register") Logout
    
    div
      table(style='width:50%', border='1px solid black')
        thead
          tr
            th Name
            th Email
        tbody 
          each user in users
            tr
              td #{user.name}
              td #{user.email}  
  
